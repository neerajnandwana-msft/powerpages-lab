# Dependencies
/node_modules

# Production build (Docusaurus output at repo root only)
/build

# Docusaurus cache and generated files
/.docusaurus
.cache-loader

# Vale: ignore downloaded style packages (run `npm run lint:prose` to fetch),
# but keep the committed project vocabulary under .vale/styles/config.
/.vale/styles/*
!/.vale/styles/config

# Misc
.DS_Store
.env
*.local
